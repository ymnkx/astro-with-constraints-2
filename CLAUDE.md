# プロジェクトルール（Claude 用・自己完結版）

このファイルは Claude Code 単独で完結するルール集。Cursor の `.cursor/rules/*.mdc` を元に、
フロントマターや `mdc:` リンクなどの Cursor 依存記法を除いて再構成している。
Cursor 側は従来どおり `.cursor/rules/` を使うため、両者は独立したコピーとして扱う
（片方を更新したらもう片方にも反映すること）。

これは Astro を基盤としたデザインシステム・フロントエンド開発プロジェクト。

---

## 詳細ルールは Skill に分離（トークン節約）

作業内容ごとの詳細ルールは Skill 化してあり、該当作業のときに本体を読み込む。
**該当ファイルを編集する前に必ず対応する Skill を参照すること。**

- **`scss-rules`** — CSS/SCSS 設計ルール（BEM 厳格版・トークン必須・rem 関数・論理プロパティ等）。`.scss` を書く/編集するとき。
- **`component-rules`** — Astro コンポーネント開発（ディレクトリ構造・Props 型定義・単一責任）。`src/components` 配下を作成/編集するとき。
- **`ts-rules`** — TypeScript/JavaScript 規約（型明示・any 禁止・命名・console.log 運用）。`.ts`/`.js` を書くとき。
- **`deploy-check`** — ビルド成果物の検査とデプロイ可否判定。デプロイ/納品/品質チェックのとき。

`.cursor/rules/*.mdc` を更新したらこれら Skill にも反映すること（逆も同様）。

---

## プロジェクト共通

### ディレクトリ・ファイル命名規則

- **ディレクトリ**: ケバブケース（例: `button-group`）
- **Astro コンポーネント**: パスカルケース（例: `TemplateComponent.astro`）
- **SCSS**: コンポーネントと同名（例: `TemplateComponent.scss`）
- **TypeScript**: キャメルケース（例: `useModal.ts`）

### 技術スタック

- Astro / TypeScript / SCSS

### デザイントークン

- カラー・スペーシング・タイポグラフィは `tokens/` を参照
- ハードコード禁止、必ずトークンを使用
- **デザイン仕様の全体像（トークン一覧・タイポグラフィ・カラー・ブレークポイント・生成パイプライン等）は `DESIGN.md` にまとまっている。デザイン関連の作業・トークンの確認・値の参照が必要なときは `DESIGN.md` を読むこと。** 生成物（`tokens/design-tokens.scss` 等）やトークン定義を変更したら `DESIGN.md` も更新すること。

### その他

- レスポンシブ対応必須
- アクセシビリティ配慮

### 絶対禁止（安全網・詳細は各 Skill 参照）

- Astro ファイル内での `<style>` タグ使用
- px 単位（border-width 以外）／ハードコーディング値／物理プロパティ（margin-left 等）
- 4 ワード以上のクラス名／`!important`／id セレクタ
- コンポーネントの境界を越えたスタイル指定（例: `.button-list .parts-button` のように別コンポーネントを子孫セレクタで上書きする。子の見た目は子側の Props/Modifier で制御し、親はレイアウトのみ担当）
- `any` 型の使用

---

## ビルド・デプロイルール

### ビルド環境

- `dist/` - テスト用ビルド / `build/` - 納品用ビルド

### ビルドコマンド

- `npm run dev` - 開発サーバー
- `npm run build` - 本番ビルド
- `npm run preview` - ビルド結果プレビュー

### デプロイ前チェック

- ビルドエラー・コンソールエラー・リンク切れがないこと（詳細は `deploy-check` Skill）

---

## コミット・バージョン管理ルール

### ブランチ戦略

- `main` - 本番環境 / `develop` - 開発環境 / `feature/*` - 機能開発

### コミットメッセージ

- プレフィックスを付与（例: `feat:`, `fix:`, `docs:`）
- 日本語 OK、変更内容を簡潔に

### レビュー

- PR ベース、承認後にマージ
- すべての Pull Request にレビュー必須、機能追加時はドキュメント更新も必須
- レビュー観点: コンポーネント設計 / CSS 命名規則 / トークン使用 / 型定義 / パフォーマンス
