---
// Card を横並びで並べるリストコンポーネント（子要素に Card を配置する）
interface Props {
  columns?: number;
}

const { columns = 3 }: Props = Astro.props;
---

<div class="card-list" style={`--this-columns:${columns};`}>
  <slot />
</div>
