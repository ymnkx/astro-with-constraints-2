---
// Button を横並び（グリッド）で並べるリストコンポーネント
// 各 Button を全幅にしたい場合は、呼び出し側で Button に isFull を渡す
interface Props {
  columns?: number;
}

const { columns = 3 }: Props = Astro.props;
---

<div class="button-list" style={`--this-columns:${columns};`}>
  <slot />
</div>
